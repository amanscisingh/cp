#include <bits/stdc++.h>
using namespace std;

int max = 1e9;

int main() {
    int n,k,p; cin >> n >> k >> p;
    int people[n], keys[k], visited[k];

    memset(visited, 0, sizeof(visited));

    for(int i = 0; i < n; i++) {
        cin >> people[i];
    }
    for (int i = 0; i < k; i++) {
        cin >> keys[i];
    }

    int ans=0;
    // pre-calculate the number of keys needed for each person
    int dp[]

    

    }


    return 0;
}